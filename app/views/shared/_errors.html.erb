<!-- Ошибки валидации. Будет выдаваться, если не будет пройдена валидация -->
<% if object.errors.full_messages.any? %>
  <div class="">
    <% object.errors.full_messages.each do |error| %>
      <li class = "text-danger"><%= error %></li>
    <% end %>
  </div>
<% end %>
